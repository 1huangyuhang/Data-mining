# 健身数据分析系统 - 服务管理指南

## 概述

本文档介绍如何使用 `start.sh` 和 `stop.sh` 脚本管理健身数据分析系统的服务启动和停止。

## 脚本功能

- **start.sh**: 一键启动后端和前端服务
- **stop.sh**: 一键停止后端和前端服务

## 脚本位置

- 启动脚本: `/Users/huangyuhang/Downloads/Test/数据分析/start.sh`
- 停止脚本: `/Users/huangyuhang/Downloads/Test/数据分析/stop.sh`

## 启动服务

### 步骤

1. 打开终端
2. 导航到项目目录:
   ```bash
   cd /Users/huangyuhang/Downloads/Test/数据分析
   ```
3. 执行启动脚本:
   ```bash
   ./start.sh
   ```

### 脚本执行流程

1. 启动后端 Spring Boot 服务
2. 等待 5 秒让后端服务完全启动
3. 启动前端 React 服务
4. 输出服务状态和访问地址

### 启动后访问地址

- 后端服务: http://localhost:8080/api
- 前端服务: http://localhost:5173/

### 查看日志

- 后端日志: `tail -f backend.log`
- 前端日志: `tail -f fitness-frontend/frontend.log`

## 停止服务

### 步骤

1. 打开终端
2. 导航到项目目录:
   ```bash
   cd /Users/huangyuhang/Downloads/Test/数据分析
   ```
3. 执行停止脚本:
   ```bash
   ./stop.sh
   ```

### 脚本执行流程

1. 查找并停止后端 Spring Boot 服务
2. 查找并停止前端 React 服务
3. 输出停止结果

## 注意事项

1. **权限问题**: 确保脚本具有执行权限
   ```bash
   chmod +x start.sh stop.sh
   ```

2. **端口占用**: 确保 8080 (后端) 和 5173 (前端) 端口未被占用

3. **依赖项**: 确保已安装所需依赖
   - 后端: Maven、Java 17+
   - 前端: Node.js、npm

4. **数据库连接**: 确保 PostgreSQL 数据库已启动并可访问

5. **日志管理**: 定期清理日志文件，避免占用过多磁盘空间

## 常见问题排查

### 后端服务启动失败

- 检查数据库连接配置
- 查看后端日志: `tail -f backend.log`
- 确认 Java 版本是否正确

### 前端服务启动失败

- 检查 npm 依赖是否已安装: `cd fitness-frontend && npm install`
- 查看前端日志: `tail -f fitness-frontend/frontend.log`
- 确认端口 5173 是否被占用

### 服务启动但无法访问

- 检查防火墙设置
- 确认服务是否在正确端口运行
- 尝试使用 `curl` 测试 API 端点: `curl http://localhost:8080/api/v1/fitness`

## 手动管理服务

### 手动启动后端服务

```bash
cd /Users/huangyuhang/Downloads/Test/数据分析
mvn spring-boot:run
```

### 手动启动前端服务

```bash
cd /Users/huangyuhang/Downloads/Test/数据分析/fitness-frontend
npm run dev
```

### 手动停止服务

查找进程 ID 并终止:

```bash
# 查找后端进程
ps aux | grep "spring-boot:run"

# 查找前端进程
ps aux | grep "npm run dev"

# 终止进程
kill <进程ID>
```

## 总结

使用 `start.sh` 和 `stop.sh` 脚本可以方便地管理健身数据分析系统的服务。这些脚本提供了一键启动和停止服务的功能，简化了开发和部署过程。

如果遇到任何问题，请参考本文档的常见问题排查部分，或查看相应的日志文件获取详细信息。